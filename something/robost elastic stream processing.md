Briskstream generates different execution plans under different input workloads.
Intitively, it generates many different plans for (infinite) different configurations.
Can we generate a small set of plans so that it can be as robost as possible in production environment?